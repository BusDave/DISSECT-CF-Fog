# Python dependencies for the GNN workflow-scheduler pipeline.
# This file is the canonical fork-reproduction entry point referenced from
# THESIS_PIPELINE.md. The upstream DISSECT-CF-Fog scripts (DAG.py, map.py,
# clusterMap.py) also use folium/pydotplus, so those are included here.
#
# Tested with Python 3.10 on macOS / Linux / Windows.
# Install with:
#     pip install -r requirements.txt

# --- Core numerics ---
numpy>=1.24
pandas>=2.0

# --- GNN training (PyTorch CPU is enough; CPU training ~5 min on 200 workflows) ---
torch>=2.0

# --- ONNX export + parity validation ---
onnx>=1.15
onnxruntime>=1.17

# --- Workflow generation (PyPI tops out at 1.4 as of 2026) ---
wfcommons>=1.3

# --- Notebook + plotting ---
notebook>=7.0
matplotlib>=3.7

# --- Upstream visualisation utilities (DAG.py, map.py, clusterMap.py) ---
# pydotplus requires a separate Graphviz binary on the system:
#   macOS:   brew install graphviz
#   Debian:  sudo apt install graphviz
#   Windows: https://graphviz.org/download/
pydotplus>=2.0
folium>=0.14
